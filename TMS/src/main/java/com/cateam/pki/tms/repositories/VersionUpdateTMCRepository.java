package com.cateam.pki.tms.repositories;

import com.cateam.pki.tms.entities.VersionUpdateTMC;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author ThucCD
 */
public interface VersionUpdateTMCRepository extends JpaRepository<VersionUpdateTMC, Long> {
}
